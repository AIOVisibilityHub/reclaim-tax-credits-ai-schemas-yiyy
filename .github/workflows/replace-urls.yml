name: Bulk URL Replace (Payroll → Main)
on: workflow_dispatch
permissions: {contents: write}

jobs:
  replace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with: 
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Replace Payroll URLs
        run: |
          # Replace BOTH old URLs with payroll.reclaimtaxcredits.com
          find . -type f \( -name "*.json" -o -name "*.md" -o -name "*.html" -o -name "*.txt" \) \
            -not -path "*/.git/*" \
            -exec sed -i \
              -e 's|reclaim-tax-credits.vercel.app|payroll.reclaimtaxcredits.com|g' \
              -e 's|https://payroll.reclaimtaxcredits.com/s|https://payroll.reclaimtaxcredits.com|g' \
              {} +

      - name: Verify changes
        run: |
          echo "Files BEFORE:"
          grep -l "reclaim-tax-credits.vercel.app" **/*.json **/*.md || echo "None"
          echo "Files AFTER:"
          grep -l "payroll.reclaimtaxcredits.com" **/*.json **/*.md || echo "None"
          git status

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --staged --quiet; then
            echo "❌ No changes detected"
          else
            git commit -m "chore: replace URLs → payroll.reclaimtaxcredits.com [skip ci]"
            git push
            echo "✅ SUCCESS - All files updated!"
          fi
